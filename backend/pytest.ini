[pytest]
addopts = --verbose --tb=short
pythonpath = .
markers =
    slow: mark tests as slow
    unit: unit tests
    integration: integration tests
    e2e: end-to-end tests
    performance: performance tests
    evaluation: evaluation tests using DeepEval
python_files = test_*.py
